import { describe, expect, it } from 'vitest';
import { renderHook } from '@testing-library/react';

describe(() => {
  it('shoudl fail until I implement this test', () => {
    expect(true).toEqual(false);
  });
});
